# 安全策略

## 支持范围

安全修复会优先合入默认分支的最新版本。请在报告时尽量说明你使用的提交、浏览器版本和部署方式。

## 如何报告问题

请不要在公开 Issue 中披露可能被利用的安全问题。仓库启用后，请优先使用 GitHub 的 **Private vulnerability reporting** / 私密安全公告渠道；如果该渠道尚未开启，请通过仓库维护者的私下联系方式报告。

一份有帮助的报告通常包含：

- 清晰、可复现的步骤或最小示例；
- 受影响的版本、浏览器和操作系统；
- 实际结果、预期结果和可能影响；
- 你认为可行的缓解或修复建议（可选）。

## 我们特别关注的范围

本项目会在浏览器端的 Web Worker / WASM 中处理不受信任的字体文件。以下问题尤其重要：

- 解析器崩溃、死循环、异常高的 CPU 或内存占用；
- 不符合预期的网络请求、数据外发或跨站资源加载；
- 可能突破浏览器安全边界的 WASM、Worker 或依赖问题；
- GitHub Pages、Docker/Nginx 部署配置导致的安全响应头或资源路径问题。

请不要在报告中附上没有授权再分发的字体文件；可以提供最小可公开样本、文件指纹或私下复现步骤。
